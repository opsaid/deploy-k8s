KUBELET_KUBEADM_ARGS="--node-ip={{ hostvars[inventory_hostname]['ip'] }} --container-runtime=remote --container-runtime-endpoint=unix:///var/run/containerd/containerd.sock --hostname-override={{ hostvars[inventory_hostname]['ip'] }}"

{% if cloud_provider == "alibaba-cloud" %}
KUBELET_CLOUD_PROVIDER_ARGS="--cloud-provider=external --provider-id={{ PROVIDER_ID }}"
{% endif %}
